name: "user-lambdafunction"
handler: "src/users/handler.users"
description: "Función lambda para USER"
events:
  - http:
      path: "users"
      method: get
  - http:
      path: "users/{id}"
      method: get
  - http:
      path: "users"
      method: post
  - http:
      path: "users/{id}"
      method: put
  - http:
      path: "users/{id}"
      method: delete
